# Changelog

This repository has no release or version yet.

## Unreleased — founding bootstrap pending review and publication

- Recorded the future repository/distribution identity `diffusiongemma-toolkit`, Python namespace `dgemma`, and optional extra spelling `[mcp]`.
- Imported selected history from ComfyUI-DiffusionGemma with repository-local provenance records.
- Marked the retained code as pre-implementation, not installable, and without a stable public API.
- Removed legacy packaging declarations rather than inventing a package contract.
- Added the settled extraction plan and a resumable publication-pending handoff.

Historical ComfyUI-DiffusionGemma release notes are not target releases and are not reproduced here. Their source history remains available in the source repository and selected filtered lineage.
