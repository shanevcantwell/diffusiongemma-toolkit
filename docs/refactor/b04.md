# B04 — scoped CPU source obligations

**2026-09-16: B04 PASS. V02 independent verification remains PENDING.**
Worker verified clean destination HEAD `ee09fc6b3cff96ebf61346ef88c32888077e6104`
before writes. Parent owns the immediate checkpoint; no commit/merge claimed.
No `dgemma/` or `surfaces/` bytes changed. Existing 17 historical tests and
A/F0/B01–B03 checkpoint records remain byte-identical.

## Provenance and exact rehomes

Source: `fed377afc7b54f03cb7faa4dd798c80c15279d8a` in
`https://github.com/shanevcantwell/ComfyUI-DiffusionGemma`.
All 27 git-lane exports were SHA-256 **and Git blob** verified before copy;
no dirty source worktree content used. Export `MANIFEST.json` SHA-256:
`54b20978812bc8f8071242a1ce0b211faafa92a342038042b99c531cba33e668`.
Source rows retain pinned origin commit/path/blob and now name their destinations:
23 byte-retained, four adapted. These are **not** also target-authored rows.
Only this record and its path shard add authored rows: 64 → 66.

Every test below is `tests/test_<stem>.py`; count is final collected cases.
Native engine/internal imports and existing monkeypatch targets remain valid.

| Stem | Cases | Adaptation |
|---|---:|---|
| capture_full_distribution_e2e | 7 | None; CPU fake, not live |
| capture_top_k_e2e | 5 | None; CPU fake, not live |
| conftest_fake_pipeline | 33 | None |
| constraints | 6 | None |
| constraints_hook | 7 | None |
| control_signals | 8 | None |
| frame_capture_discipline | 37 | None |
| hook_lifecycle | 13 | None |
| kv_cache_drive_body | 33 | None |
| kv_cache_ingress | 42 | None |
| kv_cache_run_diffusion | 17 | None |
| kv_cache_coverage_floor | 3 | Toolkit two-module allowlist only; source Comfy three cases D01 |
| loop_golden_trace | 2 | None; regeneration flag explicitly unset |
| model_load | 37 | None |
| run_diffusion_ingress | 6 | None |
| run_diffusion_statelessness | 13 | None |
| step_end_composite | 20 | None |
| seam | 4 | None |
| mcp_dual_context_import | 4 | Add canonical root identity checks; preserve native `__module__` assertions |
| mcp_encode_command | 11 | None |
| mcp_generate_command | 29 | None |
| mcp_import_guard | 6 | Comfy-only source subcase D01; all SDK failure assertions retained |
| mcp_server_dispatch | 8 | None |
| mcp_statelessness | 18 | None |
| mcp_surface_seam | 2 | None |

Also rehomed `tests/conftest.py`: only CPU fake pipeline/scheduler and synthetic
KV fixture setup, starting at `FakeFrozenConfig`, with their dataclass/type/
torch/transformers imports. Removed source introductory live-suite guidance,
`DEFAULT_REPO_ID`, `weights_cached`, and `require_live_weights`; no source config,
Comfy hooks, model caches or helper stubs transplanted. Local helper dependencies
resolve under pytest importlib mode. `tests/fixtures/loop_golden_trace.json`
retains SHA-256 `9a29af94f8019c90927c5749d56d48f2cf621b7412ea7dab3789e4ee03c7f27a`.
Expected outputs were never regenerated.

## Explicit residual ownership

- D01: source `test_mcp_import_guard.py::test_comfyui_surface_still_loads_with_mcp_sdk_absent`;
  source floor parameters `surfaces/comfyui/encode.py`, `denoise.py`, `socket_types.py`.
  Not collected here, not passing absence stubs. Real Comfy tests retain D ownership.
- `test_units_glossary_mint.py` imports real `surfaces.comfyui.sampler` for
  cross-door parity; source module deferred to D01, not transplanted.
- `test_q2_preflight.py` exercises source-only `tools/q2_preflight.py`, not toolkit
  behavior. Its tests are mocked CPU; deferral is tooling ownership (X01), **not**
  an unavailable-GPU excuse.
- Source test/fixture READMEs and count-numerals run-output fixtures remain source
  documentation/consumer material (X01). No generic analysis, audit or run-log migration.
- Packaging/install C01, real weights/live setup E01, other already-classified
  consumer/tooling/Comfy obligations retain their respective ownership. No C–F closure.

## Checker correction

The B02 checker accounted destinations from source-origin rows, but checked retained
blobs and collected modified digest paths only for target-baseline rows. Newly
rehomed source-only-origin files could therefore change without byte evidence.
`tools/check_refactor_manifest.py::check` now applies retained blob verification or
explicit modified-content digest coverage to source destinations absent from the
target baseline, and rejects invalid rehomed dispositions. No semantic waiver,
recursive hash policy change or duplicate authored row was used.

Four new `tests/test_refactor_manifest.py` cases prove retained golden/test mutation
rejection, adapted conftest mutation rejection, and missing adapted digest rejection:
`test_source_origin_rehomes_have_byte_evidence` (three parameters) and
`test_adapted_source_rehome_digest_cannot_be_omitted`. Existing negative fixtures pass.

## Commands and observations

Use the preserved CPU F0 venv and source its `safe-environment.sh`; override every
temporary/HF/XDG/torch/triton/CUDA/matplotlib/pip cache into a **fresh externally owned**
`RUN`. Disable bytecode/plugin autoload, hide GPUs, set HF/transformers offline,
unset `DGEMMA_GOLDEN_TRACE_REGEN`. No installs, environment edits, downloads or servers.
Set `DGEMMA_SOURCE_REPO="$SOURCE"`. Exact external paths/harness are in the parent report.

```sh
cd "$CANDIDATE"
"$CPU_PYTHON" -B -m pytest tests --collect-only --import-mode=importlib \
  -p no:cacheprovider --basetemp="$RUN/final-collect-temp" -q
COVERAGE_FILE="$RUN/.coverage" "$CPU_PYTHON" -B -m coverage run \
  --source=dgemma,surfaces -m pytest tests \
  --ignore=tests/test_kv_cache_coverage_floor.py --import-mode=importlib \
  -p no:cacheprovider --basetemp="$RUN/coverage-temp" \
  --junitxml="$RUN/coverage.xml" -q
COVERAGE_FILE="$RUN/.coverage" "$CPU_PYTHON" -B -m coverage json -o "$RUN/coverage.json"
# Source CoverageData() reads cwd/.coverage; keep data external without changing test semantics.
(cd "$RUN"; PYTHONPATH="$CANDIDATE" COVERAGE_FILE="$RUN/.coverage" \
  "$CPU_PYTHON" -B -m pytest "$CANDIDATE/tests/test_kv_cache_coverage_floor.py" \
  --import-mode=importlib -p no:cacheprovider --basetemp="$RUN/floor-temp" \
  --junitxml="$RUN/floor.xml" -q -ra)
"$CPU_PYTHON" -B tools/check_refactor_manifest.py --mode full --source-repo "$SOURCE"
```

Final collection: **867 cases**, including **371 rehomed** obligations; exit 0.
Measurement: **861 passed, 2 existing skips, 1 existing strict xfail**, exit 0,
86.56s. Separate floor: **3 passed**, exit 0, 0.11s; no absent-data skips.
Initial engine/MCP/rehomed run excluding floor/manifest/consumer files:
780 passed, 2 skipped, 1 xfailed, exit 0; no newly introduced unexpected failures.
Four new checker cases and all existing boundary cases are included in measurement.

Coverage: **1287/1318 statements (97.64795%)**, 31 missing, 19 excluded, no branch
measurement or global floor. Both `dgemma/kv_cache.py` (97/97) and `dgemma/types.py`
(94/94) pass the existing scoped 100% floor. All other measured modules except
`dgemma/model.py` are 100%, preserving every F0 100% module. Model gaps:
275–286, 289–291, 298–304, 309–312, 323–324, 614–615, 626–628, 675.
Not repaired, hidden by omit rules, or reclassified as a refactor regression.

Exact existing non-pass IDs (unchanged obligations, not newly discovered reds):
- `tests/test_chat_template_thinking.py::test_injected_think_message_differs_from_native_by_exactly_one_newline`
- `tests/test_chat_template_thinking.py::test_trailing_newline_in_injected_content_is_trimmed_so_parity_is_unreachable`
  — both skip for uncached tokenizer/processor in the fresh offline cache.
- `tests/test_ingress.py::TestExactTemperatureRunLevelIngress::test_t_min_equals_t_max_is_accepted`
  — existing strict xfail, source issue
  `https://github.com/shanevcantwell/ComfyUI-DiffusionGemma/issues/110`.

Full checker: **PASS/exit 0**; 347 rows (202 source, 79 target, 66 authored),
172 candidate files, 98 contracts, exact 30 exports, 74 current digests,
29 frozen files, all eight MCP consumers, **zero violations**.

## Evidence hashes and next gate

Exact collected IDs and JUnit test outcomes remain in these external artifacts;
`coverage.xml` is pytest JUnit, not a coverage-format XML file.

| Artifact | SHA-256 |
|---|---|
| preservation.json | `8cff7ff23b157af13b9634d4b87d641e0218b9f5feebf670fc5f50f33669d2e6` |
| final-collect.log | `608f5b68f44dd80fef7cc8921df4458f940202f11a0b113940370df02b6d23c9` |
| coverage.log | `ecc06733890ee4f8bf13aad397748e722f40d4218583953578bb8326c62b4e8c` |
| coverage.xml | `dc4a5bbb4f0476b208d674ad9efb1b0e6533f7da44509e9b748589a889b0f459` |
| coverage.json | `2a459da584d2d44faa1c6724455c28ca0451c0da0846e618334b7dc939dc76f0` |
| .coverage | `6c2dc7655f99091d72035fa20d47354f91289def28d6d3e1302bf2681062c34e` |
| floor.log | `a0ba7f2fb96adfb2c2564a1053a85160f60c5f008ba0c8c06a58a8cb23d1fc2b` |
| floor.xml | `a8f7c0e3e9d9e2688fed6a7f1830c6af20c163b3399a044071efe5f34960d7f9` |
| final-collect.exit / coverage.exit / floor.exit | `9a271f2a916b0b6ee6cecb2426f0b3206ef074578be55d9bc94f6f3fe3ab86aa` |
| check-initial.json (before status closure) | `66a0f77d0129ffc2440d9734d6e2e0c213a60d89900384c54bbd247c99aceeb0` |

After current status closure, refresh explicit hashes and run the read-only full
checker again; final output/hash belongs in the external parent report. Test and
production bytes remain the measured bytes. A/F0/B01–B03 historical observations
are not reinterpreted as current review.

Next: parent checkpoint, then **independent V02** on that exact commit using a new
owned run directory and preserved CPU venv: read manifest/deferrals, repeat
collection/CPU suite/coverage/floor/full checker, independently compare exact F0
non-pass IDs, unexpected-failure set and scoped 100% modules, inspect topology-only
adaptations and golden/source byte evidence. Do not infer V02 PASS from this worker
run. Non-hermetic shared-base and prior pip-check-exit-1 limitations persist;
installed/Comfy/protocol/live/release and C–F gates remain deferred.
