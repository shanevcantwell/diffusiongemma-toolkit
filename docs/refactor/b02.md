# B02 — durable enforcement and native fidelity

## Checker review corrections — 2026-09-16

Static review after checkpoint `1901549ce3261fdfad1e434689abea06d1c8bc0d`
found two P2 false negatives. Before editing, both exact counterexamples returned
`[]` and failed external regression assertions (2 failed, exit 1):

```python
import dgemma as dg
dg.model.load_model()
import json as dg
```

```python
import runpy
runner = runpy.run_module
runner('dgemma.model')
```

Imports and assignments now accumulate possible bindings rather than overwrite
them across rebinding, sibling functions or branches. This is deliberately
scope/flow-insensitive conservative analysis, not Python execution semantics.
Assignments retain finite engine representatives (root member plus first private
traversal) and exact dynamic modules/primitives; monotone propagation terminates
even with self-referential or mutually recursive attribute aliases. Public root
aliases remain permitted. One primitive set drives both propagation and call
inspection: importlib.import_module, __import__, eval/exec (including bound
builtins), and runpy.run_module/run_path. Module and chained assignment aliases
are followed. Exact expression + package + nonempty rationale dispositions remain
required; an internal module-name literal is still unwaivable. Real consumers
receive no dispositions; consumer census and production bytes are unchanged.

Observed corrected-code results in the preserved CPU F0 venv, with fresh external
cache/basetemp/logs, offline/no-GPU/no-bytecode/no plugin autoload:

- Consumer fixtures plus the two external exact repros: **141 passed**, exit 0.
- Focused root/manifest/consumer/native/MCP tests: **306 passed in 24.08s**, exit 0.
- Read-only full checker: **PASS**, exit 0; 347 path rows, 172 candidate files,
  74 current digests, 98 contracts, 30 exports, 29 frozen files, 8 MCP consumers,
  zero violations. No new candidate files or consumer scope changes.

```sh
"$CPU_PYTHON" -B -m pytest tests/test_root_boundary.py \
  tests/test_refactor_manifest.py tests/test_consumer_edges.py \
  tests/test_native_fidelity.py tests/test_mcp_boundary.py \
  --import-mode=importlib -m 'not live and not e2e' -p no:cacheprovider \
  --basetemp="$RUN/focused-basetemp" --junitxml="$RUN/focused.xml" -q
"$CPU_PYTHON" -B tools/check_refactor_manifest.py --mode full \
  --source-repo "$SOURCE"
```

External evidence run: `checker-corrections-su9Ef4EN` (worker handoff records the
location; not a runtime dependency). SHA-256 of raw observations:

| Artifact | SHA-256 |
|---|---|
| repro.log (before edits, 2 failed) | `91038120c694210ed8c9d5699bb9c0b2a95237612717b1206e2106f51e7bcb37` |
| consumer-final.log | `f73293214f393cc51421c4387a9c44095ca1e4020c7989694cd629ddd4112633` |
| focused.log | `b24b71fabed479314db51f1c3903c6851b314ec9938af4dc016f62ed0580c932` |
| focused.xml | `e46d55e9479179223fff261b924d3e926ba17fffdc440b5bf3ece6d9651ad656` |
| checker-before-records.json | `66a0f77d0129ffc2440d9734d6e2e0c213a60d89900384c54bbd247c99aceeb0` |

Code/test bytes are unchanged after those passes; record/digest closure is checked
separately by the final read-only full checker (raw results in worker evidence).
The earlier independent V02 run against `1901549` is **pre-correction** evidence:
867 collected, measurement 861 passed / 2 skipped / 1 xfailed, separate floor
3 passed. Those successful observations remain historical; final current-candidate
independent rereview and V02 rerun are **PENDING**, not closed by this repair.
No installs, environment mutations, native bug fixes or C–F work are authorized.

## Historical B02 checkpoint (before B03/B04 and the corrections above)

**2026-09-16: B02 implementation and focused tests PASS.** B03/B04/V02 remain PENDING. Actual retained MCP consumer gate **FAIL**, intentionally not waived. Parent verified the clean synchronized B01 checkpoint `f54f270f849ba0eee2fd6bc1ba61ed49f775b5ad` before this work; no new checkpoint/publication is claimed here.

The stdlib-only read-only `tools/check_refactor_manifest.py` accepts `--repo` and
`--source-repo` (defaults to candidate only if pinned source objects exist there).
It checks immutable source/target trees and blobs, current candidate accounting,
the 67-path historical projection, frozen ADR/provenance/LICENSE, schema-shaped
records, task/evidence references, cycles, native/current AST contracts, exact 30
root bindings, and explicit nonrecursive content coverage. No evidence is minted
by the checker. Historical A04 evidence is historical, not current-candidate proof.

`--mode manifest-only` intentionally does not run the actual consumer gate.
`--mode full` also scans accounted current consumer destinations, resolving
namespace/relative imports and both bundled/standalone contexts. Direct root
names/aliases and adapter siblings are allowed; internal edges, wildcard imports,
private traversal and undisposed dynamic imports fail. Engine transitive imports
are not consumer edges. No dynamic disposition is granted to real consumers.
Source-only Comfy rows remain absent obligations until D; they are not silently
certified by this candidate scan.

The retained MCP adapter still imports internals: full mode is expected to FAIL
until B03. Fixture tests asserting those diagnostics are not actual edge PASS.

Focused tests include isolated negative candidate copies, AST positive/negative
fixtures, all four loader callback phases for both quants, processor-only native
exception translation/chaining, callback/retention identity, budget-gated full
softmax, uncapped direct collector versus public ingress, raw decode and native
encode/KV aliasing/forwarding. Native defects remain unchanged. No production
files beyond B01 were changed; no packaging, model downloads, installs, source
rehoming, baseline/full-suite rerun, Comfy conversion or live certification.

## Commands and observed results

Source the preserved CPU F0 `safe-environment.sh`; use its explicit interpreter,
then override all temporary/cache locations into a newly allocated external owned
run directory. Bytecode/plugins disabled, CUDA hidden, HF/transformers offline,
fresh empty HF cache. No installs or modifications to the preserved environment.

```sh
"$CPU_PYTHON" -B -m pytest tests/test_root_boundary.py \
  tests/test_refactor_manifest.py tests/test_consumer_edges.py \
  tests/test_native_fidelity.py --import-mode=importlib \
  -m 'not live and not e2e' -p no:cacheprovider \
  --basetemp="$RUN/basetemp" --junitxml="$RUN/focused.xml" -q
"$CPU_PYTHON" -B tools/check_refactor_manifest.py \
  --repo "$CANDIDATE" --source-repo "$SOURCE" --mode manifest-only
"$CPU_PYTHON" -B tools/check_refactor_manifest.py \
  --repo "$CANDIDATE" --source-repo "$SOURCE" --mode full
```

Tests use `DGEMMA_SOURCE_REPO="$SOURCE"` for pinned source object reads; unset
means the candidate must contain those objects. Git calls are deterministic
read-only tree/object/index queries, never repository management or mutations.
No external authoring script or environment path is a checker dependency.

**184 passed in 18.55s, exit 0** (63 existing root tests plus 121 new focused
checks). Manifest-only: **PASS**, 343 path rows (202 source, 79 target, 62 authored),
141 current candidate files, 98 globally unique contracts, exact 30 root exports,
64 explicit current content hashes, five exclusions, 29 frozen baseline files
(including 17 ADRs). Historical 67-path projection and byte-preserved A04 index
remain unchanged. Current content is measured, not asserted to equal historical A.

Full candidate+consumer mode: **FAIL, exit 1**, 48 private-edge diagnostics at
12 import statements in `surfaces/mcp/commands/generate.py` and
`surfaces/mcp/state_manager.py`; all eight accounted MCP Python files scanned.
Both branches are visited. This is expected pending B03, not an exemption.

External run artifacts (locations are in the worker handoff, not runtime inputs):

| Artifact | SHA-256 |
|---|---|
| focused.log | `2459b2245e6c8c6ab385ca46b561d96ab8d049e6367a18127859fe8efd74dfd2` |
| focused.xml | `14aff35e9182ab3be6858a70903fdb36be6d083b2a17ba35e88ee4537750d649` |
| exit-code.txt | `9a271f2a916b0b6ee6cecb2426f0b3206ef074578be55d9bc94f6f3fe3ab86aa` |
| full-before-records.json | `228d76bfa0c45e3598333a66c1a545965d35edc97bab6ac88f3beaf99e88ae47` |

The test result above precedes status-record closure; code/test bytes are unchanged.
Record edits are covered by refreshed explicit hashes and subsequent read-only
checker execution, not retroactively claimed as independently reviewed A04 bytes.
The checker cannot authenticate narrative evidence; PASS requires actual referenced
observations and non-planned tests, and cannot be inferred from collection failure.
Original baseline and shared-base/non-hermetic/pip-check limitations remain.

## Next / deliberately not established

Parent owns immediate review/checkpoint/publication. B03 must redirect retained
MCP without fixing known defects, then replace the tests that explicitly observe
pending real edges with actual PASS assertions. B04 rehomes reviewed source test
obligations; V02 performs the later post-boundary baseline comparison. Cancellation,
hook teardown and per-run ownership follow-through remain B03/B04/V02 obligations;
the focused loader/frame/encode tests do not replace them. C–F remain deferred.
No all-tests, independent review, full boundary, installed artifact, Comfy, protocol,
live, release or dependency-health PASS is claimed.
