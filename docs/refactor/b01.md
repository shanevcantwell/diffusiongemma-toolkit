# B01 — canonical root re-exports

**2026-09-16: PASS, focused checkout evidence only.** Parent git lane verified the isolated candidate clean at `94cada0b77037a59721ee99954abe21f0b90eebe` before implementation. Parent owns subsequent checkpoint/push and issue transport; this record makes no new commit claim.

## Change and enforcement

Only production change: `dgemma/__init__.py`. The exact ordered 30-name `contracts.json` allowlist preserves the existing 16 exports. Native functions/classes/constants are imported directly: `decode_frames` from `.excision`, `QUANT_CHOICES` from `.model._QUANT_CHOICES`. Existing `.loop`-before-`.model` import order is retained. No wrappers, lazy imports, native definition changes, loader redesign or dependency stubs.

`tests/test_root_boundary.py` enforces:

- exact root `__all__`, star-import set and canonical `is` identity for all 30 names;
- five native signatures, ordered parameter kinds, contract defaults, default-object identity, raw/resolved annotations and `__module__`;
- twelve dataclasses' native field identity/order/defaults, constructor signatures, resolved annotations and frozen flags;
- all twelve constants, including native mutable `KNOB_DOCS` identity and mutation visibility;
- canonical `LoadInterrupted`, direct `Exception` base and inherited constructor;
- fresh subprocess root import and native CPU decode/fingerprint calls while a meta-path blocker makes MCP/Comfy and adapter imports unavailable. Explicit failed import probes establish that the blocker is effective; attempted framework imports during root use also fail the test. This is not a transitive-internal-module ban.

## Actual command and result

Using the already provisioned CPU F0 interpreter, sourced safe environment, new run-owned external output/cache directories and a new empty offline HF cache:

```sh
"$CPU_PYTHON" -B -m pytest tests/test_root_boundary.py \
  --import-mode=importlib -m 'not live and not e2e' -p no:cacheprovider \
  --basetemp="$RUN/basetemp" --junitxml="$RUN/root-tests.xml" -q
```

**63 passed in 8.72s; exit 0.** Bytecode and plugin autoload disabled; CUDA hidden; HF/transformers offline. No installs/downloads, global mutations, real weights or server actions. No baseline rerun. Existing shared-base/non-hermetic limitations and recorded pip-check failure remain; no dependency-health claim.

External run artifact identities (locations retained in the implementation handoff, not required at runtime):

| Artifact | SHA-256 |
|---|---|
| root-tests.log | `84e4c4951edcffa95bb5f10e5fd370a64565fe6ec1accebf8343a165b599b6fe` |
| root-tests.xml | `5b9594007a4d3c989445851e76d292731b3f356f22c3cfe32abe63b77c701dc8` |
| exit-code.txt | `9a271f2a916b0b6ee6cecb2426f0b3206ef074578be55d9bc94f6f3fe3ab86aa` |

## Accounting and historical evidence

Current census: 333 rows (202 source baseline, 79 target baseline, 52 target-authored), 131 candidate files. New paths: this record, the focused test, and `evidence-content-a04.json`. Both root provenance rows retain their immutable origin blobs and now record a modified destination. `historical-selection.json` remains the unchanged 67-path founding projection.

`evidence-content-a04.json` is a byte-for-byte archive of the previous digest index. EV-A observations remain historical; they do not claim to assess the new files. `evidence-content.json` is the explicit current B01 content coverage set, excluding both digest indexes and evidence JSON records to prevent recursive hashing. Its static readback is author bookkeeping, not independent A04 or future B02 checker certification. Both V01 baseline records and artifacts remain unchanged.

Author bookkeeping readback (refresh followed by non-refresh): **PASS** — 373 schema objects, 333 unique rows, 131 filesystem candidate files, 72 unchanged target-baseline blob hashes (including all 17 frozen ADRs, founding provenance and license), 54 explicit current digests and five exclusions. No Git command was run by this worker: pinned tree/object queries and final Git state remain parent git-lane responsibilities. Historical archive SHA-256: `e7d015bc510d1b38e1db4eb272ded85aa644f114bf72bf6b85b47459a5786741`. This readback does not supply B02's durable checker or negative-fixture gate.

## Not established / next

**B02, B03, B04 and V02 remain PENDING.** No durable manifest checker or negative fixtures, direct-consumer AST enforcement, detailed native callback/loader regressions, test rehoming or post-boundary baseline comparison is claimed. Retained MCP still contains internal imports pending B03; its actual consumer gate is **not PASS**. The root isolation test is only one enforcement axis, not an installed-artifact test. C–F (packaging, installed Comfy, protocol/live and publication) remain deferred. Native defects and ownership remain unchanged.
